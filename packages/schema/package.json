{
  "name": "@versa/schema",
  "version": "2.1.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "ts-node build.ts && tsup src/index.ts --format cjs,esm --dts",
    "dev": "ts-node build.ts && tsup src/index.ts --format cjs,esm --dts",
    "prepare": "pnpm build"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "@types/node": "^22.1.0",
    "jest": "^28.1.1",
    "json-schema-to-typescript": "^14.0.5",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.9.2",
    "tsconfig": "workspace:*",
    "tsup": "^8.2.4"
  }
}
